// Write your fasterShip object literal below
let fasterShip = {
  'Fuel Type': 'Turbo Fuel',
  color: 'silver'
};

// after declaring a variable: using curly braces, {}, to designate an object literal.
//data organized into keyvalue pairs.
//a key (variable) name that points to a location in memory that holds a value.
//Bringing in an identifier followed by a colon.
