const entree = 'Enchiladas'
console.log(entree);
entree = 'Tacos'

//const or constant: if reassigned to a const variable, the result would be typeError.
//it must be assigned a value when declared. If needing to reassign the variable, use let.
