function monitorCount(rows, columns) {
  return rows * columns;
};
const numOfMonitors = monitorCount(5, 4);
console.log(numOfMonitors);


//when the function is called, the computer runs through the function's code and evaluate the result of calling the function. result = undefined.
//So we use the return statement to pass back information from the function call.
