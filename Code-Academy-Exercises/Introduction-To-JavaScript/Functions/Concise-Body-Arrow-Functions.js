const plantNeedsWater = day => day === 'Wednesday' ? true : false;


//concise body arrow functions: being able to list one or more parameters followed by the fat arrow.
//single param is not enclosed with ().
