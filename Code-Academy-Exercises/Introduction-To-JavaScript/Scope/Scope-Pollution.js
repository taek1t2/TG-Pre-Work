const satellite = 'The Moon';
const galaxy = 'The Milky Way';
let stars = 'North Star';

const callMyNightSky = () => {
  stars = 'Sirius';
	return 'Night Sky: ' + satellite + ', ' + stars + ', ' + galaxy;
};

console.log(callMyNightSky());
console.log(stars);



//Global namespace: allows the vairables to be accessible from anywhere in the code.
//Scope pollution is when too many global variables that exist in the global namespace.
