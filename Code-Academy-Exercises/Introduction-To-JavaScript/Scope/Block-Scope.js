const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
};

logVisibleLightWaves();

console.log(lightWaves);


//block scope= only accessible to the lines of code within that block.
//variables that are declared with block scope are known as local variables b/c it only exist in the same block.
