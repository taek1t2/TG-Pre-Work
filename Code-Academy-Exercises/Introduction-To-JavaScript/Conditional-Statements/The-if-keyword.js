let sale = false;
if (sale) {
  console.log('Time to buy!');

}

//using if keyword followed by the () and then followed by the {}. Asking if the condition is a boolean.
// if true, the code in {} would execute. if false, it'll show an error or won't execute.
