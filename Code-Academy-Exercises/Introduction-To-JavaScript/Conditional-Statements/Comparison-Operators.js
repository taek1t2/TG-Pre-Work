let hungerLevel = 7;
if (hungerLevel > 7) {
  console.log('Time to eat!')
} else {
  console.log('We can eat later!')
};


//comparison operators: use different types of operators to compare values.
// values: >, <, <=, >=, ===, ==, !==
